<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.team.bf.dao.MemberDAO">
  	<insert id="memberInsert">
  		insert into member(userid, userpassword, date_birth, grade_member, grade, question, answer) 
  		values( #{userid}, #{userpassword},  #{date_birth}, ${grade_member}, ${grade}, #{question}, #{answer})
  	</insert>
  </mapper>